"""Flexisip MCP Server package.

Exposes Flexisip diagnostic tools via the Model Context Protocol (MCP)
so that any MCP-compatible AI assistant (Augment, Claude Desktop, Cursor, etc.)
can call them directly with typed parameters.
"""
